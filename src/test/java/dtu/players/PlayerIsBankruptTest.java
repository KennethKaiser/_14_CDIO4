package dtu.players;

import dtu.board.Board;
import dtu.board.FieldProperty;
import dtu.board.Property;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

/*
class PlayerIsBankruptTest {

    Board board = new Board();

    @Test
    void playerIsBankruptTest(Player player) {

        PlayerHandler playerHandler = new PlayerHandler();

        player.setMoney(-1);
        FieldProperty property = new FieldProperty(board.getCurrentBoard()[1].getProperty());
        player.setProperties(property);

        playerHandler.playerIsBankrupt(player);

        assertEquals()






    }
}

 */
